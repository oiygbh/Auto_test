name: Apifox CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:  # 手动触发选项

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js environment
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install Apifox CLI
        run: npm install -g apifox-cli

      - name: Running Test Scenario
        env:
          APIFOX_ACCESS_TOKEN: ${{ secrets.APIFOX_ACCESS_TOKEN }}
        run: apifox run --access-token $APIFOX_ACCESS_TOKEN -t 5070833 -e 24543324 -n 1 -r html,cli -o ./report.html  # 生成 HTML 报告

      - name: Archive test results
        uses: actions/upload-artifact@v3
        with:
          name: apifox-test-report
          path: ./report.html

      - name: Send report link to Slack
        run: |
          curl -X POST -H 'Content-type: application/json' --data \
          '{
            "text": "Apifox 测试完成，查看报告：[GitHub Actions](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          }' \
          https://imtest.fc.plus/api/third/send_slack_robot  # 使用你的Slack机器人链接
